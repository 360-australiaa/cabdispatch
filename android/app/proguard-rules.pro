# Add project specific ProGuard rules here.
# Sibling agents: kotlinx.serialization and Retrofit models generally need
# @Serializable classes kept — add -keep rules here once the DTO/model set is
# finalized. Left minimal for the skeleton.
